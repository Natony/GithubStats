#include <iostream>
#include <list>

using namespace std;

list<int> sortLinkedList(list<int> l) {
    for(list<int>::iterator it = lst.begin(); it != lst.end(); ++i){
        
    }
}

int main()
{
    list<int> l = {4,6,3,2};
    list<int> lst = sortLinkedList(l);
    for(list<int>::iterator it = lst.begin(); it != lst.end(); ++i)
    {
        cout << *it << " " ;
    }

}